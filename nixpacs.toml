[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["cd prototype/simple-backend && npm install"]

[phases.build]
cmds = ["cd prototype/simple-backend"]

[start]
cmd = "cd prototype/simple-backend && npm start"